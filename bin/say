#!/bin/bash
# Slaygent Communication System - Cross-platform TTS Tool
# Enhanced Bash wrapper for Python-based TTS system

# Configuration - make this portable
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"
PYTHON_SCRIPT="$SCRIPT_DIR/say.py"

# Check if Python script exists
if [ ! -f "$PYTHON_SCRIPT" ]; then
    echo "❌ Error: Python script not found at $PYTHON_SCRIPT"
    echo "Make sure you're in the Slaygent Communication System directory"
    exit 1
fi

# Show help if no arguments or --help
if [ $# -eq 0 ] || [ "$1" = "--help" ] || [ "$1" = "-h" ]; then
    echo "Slaygent Communication System - Cross-platform TTS Tool"
    echo ""
    echo "Usage:"
    echo "  say <text>                   Speak text using default voice"
    echo "  say <text> --voice <voice>   Speak with specific voice"
    echo "  say --list                   List available voices"
    echo "  say --status                 Show TTS server status"
    echo ""
    echo "Examples:"
    echo "  say 'Hello from terminal'"
    echo "  say 'System alert' --voice danny --speed 0.8"
    echo "  say --list"
    echo ""
    exit 0
fi

# Try different Python executables in order of preference
PYTHON_EXES=("python3" "python" "py")
PYTHON_CMD=""

for python_exe in "${PYTHON_EXES[@]}"; do
    if command -v "$python_exe" >/dev/null 2>&1; then
        # Test if it's Python 3
        if "$python_exe" -c "import sys; sys.exit(0 if sys.version_info >= (3, 8) else 1)" 2>/dev/null; then
            PYTHON_CMD="$python_exe"
            break
        fi
    fi
done

if [ -z "$PYTHON_CMD" ]; then
    echo "❌ Error: Python 3.8+ not found"
    echo "Please install Python 3.8 or later and ensure it's in your PATH"
    exit 1
fi

# Execute the Python script with all arguments
exec "$PYTHON_CMD" "$PYTHON_SCRIPT" "$@"